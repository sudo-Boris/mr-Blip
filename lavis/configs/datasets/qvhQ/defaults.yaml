 # Copyright (c) 2022, salesforce.com, inc.
 # All rights reserved.
 # SPDX-License-Identifier: BSD-3-Clause
 # For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/BSD-3-Clause

datasets:
  qvhQ: # name of the dataset builder
    data_type: videos # [images|videos|features]
    build_info:
      # Be careful not to append minus sign (-) before split to avoid itemizing
      annotations:
        train:
          url: Your/path/to/train_mcqa.json
          storage: Your/path/to/train_mcqa.json
        val:
          url: Your/path/to/val.json
          storage: Your/path/to/val.json
        test:
          # url: Your/path/to/test_dummy.json
          # storage: Your/path/to/test_dummy.json
          url: Your/path/to/val.json
          storage: Your/path/to/val.json
      videos:
        storage: Your/path/to/data/raw/QVHighlights
